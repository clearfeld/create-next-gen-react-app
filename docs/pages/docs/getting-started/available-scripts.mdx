# Available Scripts

import { Tab, Tabs } from 'nextra-theme-docs'

In the project directory, you can run:

## `start`

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    ```bash copy
    npm run start
    ````
  </Tab>
  <Tab>
    ````bash copy
    pnpm start
    ````
  </Tab>
</Tabs>

Runs the app in the development mode. Your browser will automatically open to http://localhost:3000.

The page will reload if you make edits.

## `test`

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    ```bash copy
    npm run test
    ````
  </Tab>
  <Tab>
    ````bash copy
    pnpm test
    ````
  </Tab>
</Tabs>

Launches the test runner in the interactive watch mode. See the section about running tests for more information.

## `build`

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    ```bash copy
    npm run build
    ````
  </Tab>
  <Tab>
    ````bash copy
    pnpm build
    ````
  </Tab>
</Tabs>

Builds the app for production to the `dist` folder. It correctly bundles React in production mode and optimizes the build for the best performance.

{/* The build is minified and the filenames include the hashes. If necessary, classnames and function names can be enabled for profiling purposes. See the production build section for more information. */}

Your app is ready to be deployed! See the section about [deployment](/docs/deployment/deployment) for more information about deploying your application to popular hosting providers.
